description "uWSGI instance to serve <%= @app_name %>"

start on runlevel [2345]
stop on runlevel [!2345]

setuid <%= node['deploy']['deploy_user'] %>
setgid <%= node['deploy']['deploy_group'] %>

script
  cd <%= @deploy_path %>
  . .virtualenv/bin/activate
  uwsgi --ini uwsgi.ini
end script